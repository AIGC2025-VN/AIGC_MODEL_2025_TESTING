# AIGC_MODEL_2025_TESTING
---------------------------

-This our trial model which is used in AIGC 2025.
-Please down load all the file having the name `ppe`, and push it in a folder having a name `model` for perfect execution.
-`final_test_pt` and `webcam` are the files that are used to deploy model. You can choose one to run and test, or you can have both 2 files running at the same time :))) (will cause error).
